# Public build inputs only. Obtain the exact package version from the chosen
# universal image's signed apt repository and record it in the release review.
REPLAY_VERSION=replace-with-immutable-commit
REPLAY_FFMPEG_PACKAGE_VERSION=replace-with-exact-debian-package-version
